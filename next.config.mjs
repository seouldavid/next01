/** @type {import('next').NextConfig} */
const nextConfig = {
    //     reactStrictMode: false, // Strict Mode 비활성화
    //     swcMinify: true,        // SWC 최적화 활성화
    //     images: {
    //         domains: ['example.com'], // 외부 도메인 허용
    //     },
    //     env: {
    //         CUSTOM_API_URL: 'https://api.example.com', // 환경 변수 추가
    //     },
    //     output: 'standalone', // 서버리스 환경 설정
    // };

    // if (process.env.NODE_ENV === 'development') {
    //     console.error = () => { }; // 개발 중 console.error 무시
    //     console.log = () => { };
    //     console.warn = () => { };
}

export default nextConfig;
